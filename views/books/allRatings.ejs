<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ratings for Book</title>
    <link rel="stylesheet" href="/public/css/global.css">
    <link rel="stylesheet" href="/public/css/ratings.css">
</head>
<body>
<div class="container">
    <h1>Ratings for this Book</h1>

    <% if (ratings.length > 0) { %>
        <div class="ratings-list">
            <% ratings.forEach(rating => { %>
                <div class="rating-item">
                    <p><strong>User ID:</strong> <%= rating.user_id %></p>
                    <p><strong>Rating:</strong> <%= rating.rating %> / 5</p>
                    <p><strong>Review:</strong> <%= rating.review %></p>
                    <p><strong>Created At:</strong> <%= rating.created_at %></p>
                    <hr>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <p>No ratings for this book yet.</p>
    <% } %>

    <a href="/library/book/<%= bookId %>" class="btn btn-primary">Back to Book</a>
</div>
</body>
</html>
