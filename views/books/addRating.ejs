<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rate Book</title>
    <link rel="stylesheet" href="/public/css/global.css">
    <link rel="stylesheet" href="/public/css/addRatings.css">
</head>
<body>
<div class="container">
    <h1>Rate and Review Book</h1>

    <!-- Display the book title and cover -->
    <div class="book-info">
        <img src="<%= book.cover_image %>" alt="<%= book.title %>" class="book-cover">
        <h2 class="book-info"><%= book.title %></h2>
        <p><strong>Author:</strong> <%= book.author_name %></p>
    </div>

    <!-- Rating form -->
    <div class="review-rating-container">
        <form action="/ratings/<%= book.google_id %>/add" method="POST" class="rating-form">
            <input type="hidden" name="bookId" value="<%= book.id %>">
            <input type="hidden" name="googleBookId" value="<%= book.google_id %>">

            <div class="form-group review-section">
                <label for="review">Review:</label>
                <textarea name="review" id="review" rows="5" placeholder="Write your review here..." required></textarea>
            </div>

            <div class="rating-submit-container">
                <div class="form-group rating-section">
                    <label for="rating">Rating (1 to 5):</label>
                    <select name="rating" id="rating" required>
                        <option value="" disabled selected>Select rating</option>
                        <% for (let i = 1; i <= 5; i++) { %>
                            <option value="<%= i %>"><%= i %></option>
                        <% } %>
                    </select>
                </div>

                <button type="submit" class="btn btn-primary">Submit Rating</button>
            </div>
        </form>
    </div>

    <!-- Back button -->
    <a href="/library/<%= username %>" class="btn btn-secondary">Back to Book</a>
</div>

</body>
</html>
