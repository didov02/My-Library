<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Digital Library - Home</title>
    <link rel="stylesheet" href="/public/css/global.css">
    <link rel="stylesheet" href="/public/css/index.css">
</head>
<body>
<div class="container">
    <div class="welcome-message">
        <% if (username) { %>
            <h1>Welcome, <%= username %>!</h1>
        <% } else { %>
            <h1>Welcome to the Digital Library!</h1>
        <% } %>
        <p>Explore your library, search for new books, or manage your profile.</p>
    </div>

    <div class="btn-container">
        <a href="/library" class="btn btn-primary">Go to Your Library</a>
        <a href="/books/search" class="btn btn-primary">Search for Books</a>
        <a href="/users/profile" class="btn btn-primary">View Profile</a>
        <% if (username) { %>
            <form action="/users/logout" method="POST">
                <button type="submit" class="btn btn-secondary">Logout</button>
            </form>
        <% } %>
    </div>
</div>
</body>
</html>
